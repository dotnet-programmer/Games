<UserControl x:Class="Snake.WpfApp.Views.UserControls.GameOverMenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:Snake.WpfApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
			 mc:Ignorable="d"
			 d:DesignHeight="500"
			 d:DesignWidth="500">

	<Grid Background="Black">

		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="*" />
			<RowDefinition Height="*" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0"
				   Text="Game over!"
				   Style="{StaticResource TxtTopMenuPositionsStyle}"
				   Foreground="Red" />

		<TextBlock Grid.Row="1"
				   Style="{StaticResource TxtMenuPositionsStyle}"
				   Foreground="YellowGreen">
		<Run Text="Your score:" />
		<Run Text="{Binding CurrentScore, Mode=OneWay}" />
		</TextBlock>

		<Button Grid.Row="2"
				Content="Play again"
				Command="{Binding StartNewGameCommand}"
				Style="{StaticResource BtnMenuStyle}" />

		<Button Grid.Row="3"
				Content="Main menu"
				Command="{Binding ShowMainMenuCommand}"
				Style="{StaticResource BtnMenuStyle}" />
	</Grid>
</UserControl>